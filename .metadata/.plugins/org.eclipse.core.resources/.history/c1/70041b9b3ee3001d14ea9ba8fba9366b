package karTasks;

import java.util.ArrayList;
import java.util.List;

public class Stringg {

	static void rev()
	{
		String s="Karthi234rth351k45c",rev="",temp="",temp2="",r="";

		//		ArrayList<Integer> list1=new ArrayList<Integer>();
		//		ArrayList<String> list2=new ArrayList<String>();

		StringBuilder sb = new StringBuilder(s.length());
		
		for(int i=0;i<s.length();i++)
		{
			int j=0;
			char c=s.charAt(i);
			 if (Character.isLetter(c))
			 {
		            sb.insert(j, c); // append in "reverse" mode
		        } 
			 else 
			 {
		            sb.append(c);
		            j = i + 1;  // store the last position of a non-digit
		        }
			
		}
		
		System.out.println(sb.toString());
	}
			
			
			
			
			
			
			
			
			
			
			
			
			
			
			
//			
//			if(Character.isLetter(s.charAt(i)))
//			{
//
//				temp+=s.charAt(i);
//			}	
//
//		else if(i<s.length()-1 && Character.isDigit(s.charAt(i)))
//			{
//				temp2+=s.charAt(i);				
//			}			
//		}
//		
//		r+=new StringBuilder(temp).reverse()+temp2;	
//		//rev=r+temp2;
//		System.out.println(r);
//	}

	public static void main(String[] args) {

		/*		String s="Karthi234rth351k45c",temp="";
		int sum=0;

		for(int i=0;i<s.length();i++)
		{
			if(Character.isDigit(s.charAt(i)))
			{
				if (Character.isDigit(s.charAt(i+1))) 
				{
					temp += s.charAt(i);
				}

				else 
				{
					temp += s.charAt(i);
					sum += Integer.parseInt(temp);
					temp = "";
				}					 						 
			} 			
		}
		System.out.println(sum);

		 */

		rev();
	}
}
